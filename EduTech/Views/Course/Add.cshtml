@{
    ViewData["Title"] = "Thêm khóa học";
}

@model EduTech.Models.AddCourseViewModel

    <div class="main-container">
@*         <a href="@Url.RouteUrl("dashboard")" class="btn btn-outline-primary mb-3">Dashboard</a>
        <a href="/dashboard" class="btn btn-outline-primary mb-3">
        *@ 
        <a href="@Url.Action("Main", "Course")" class="btn btn-outline-primary mb-3">
            <i class="bi bi-arrow-left"></i> Quay lại
        </a>
        <h3 class="mb-3 fw-normal">Thêm khóa học</h3>
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div> <!-- Hiển thị lỗi chung -->

            <div class="form-floating mb-2">
                <input type="text" class="form-control" id="floatingInput" asp-for="Name" />
                <label for="floatingInput">Tên khóa học</label>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-floating mb-3">
                <textarea class="form-control" id="floatingDescription" style="height: 120px;" asp-for="Description"></textarea>
                <label for="floatingDescription">Mô tả</label>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <button class="btn btn-primary" type="submit">Submit</button>
        </form>
    </div>
